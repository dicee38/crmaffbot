DATABASE_URL=postgresql+asyncpg://crm:crm@localhost:5432/crm
DEDUP_WINDOW_MINUTES=15
# Same token as bot/.env — lets backend push Telegram notifications directly (§4.4).
BOT_TOKEN=
# Must match bot/.env's INTERNAL_API_SECRET — see backend/deps.py.
INTERNAL_API_SECRET=change-me
LARGE_DEPOSIT_THRESHOLD=1000
DIGEST_HOUR_UTC=6
IDLE_DAYS_THRESHOLD=3
